{
  "name": "birdwatch-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sandbox": "ampx sandbox --no-browser",
    "deploy": "ampx pipeline-deploy --branch main",
    "codegen": "ampx generate outputs --app-id dummy --branch main --out-dir ."
  },
  "dependencies": {
    "@aws-amplify/backend": "^1.20.0",
    "aws-cdk-lib": "^2.237.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0"
  }
}
